interface:
  display_name: "Implement spec"
  short_description: "Drive a spec's tickets to closed one at a time: a subagent round per ticket, gated, then landed"
policy:
  allow_implicit_invocation: false
